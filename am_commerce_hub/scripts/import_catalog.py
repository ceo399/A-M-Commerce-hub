"""?????????CSV/Excel?????? CLI?

???:
  python -m scripts.import_catalog --file ????????_????.xlsx
  python -m scripts.import_catalog --file catalog.csv --sheet ?????

???????????????_????????.xlsx?????????
??????????????????????????
"""
from __future__ import annotations

import argparse
import json

from app.db.session import init_db, session_scope
from app.services import catalog_service


def main() -> None:
    p = argparse.ArgumentParser()
    p.add_argument("--file", required=True, help="CSV ??? .xlsx ???")
    p.add_argument("--sheet", default=None, help="xlsx??????????: ??????")
    args = p.parse_args()

    init_db()
    with session_scope() as s:
        report = catalog_service.import_from_file(s, args.file, sheet=args.sheet)

    print(json.dumps(report, ensure_ascii=False, indent=2))
    pr = report["parse"]
    print(f"\n????: ?{pr['total']}? / ??{pr['ok']} / "
          f"???{pr['errors']} / ??{pr['warnings']}")
    print(f"  ? ???(??????){report['imported']['new_products']}? / "
          f"??{report['imported']['discontinued']}?")


if __name__ == "__main__":
    main()
